dist: focal

language: node_js
node_js:
  - 14
cache: yarn

jobs:
  include:
    - stage: Build
      script:
        - yarn package
        - yarn dist

deploy:
  provider: snap
  snap: dist/ozone-display*.snap
  channel: edge
  skip_cleanup: true
